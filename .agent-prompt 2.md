# 🧪 QA Validation Task

## Validating: BUG P1: Garmin sync not fetching today's activities
**Original agent:** devops
**Priority:** 🟠 High

## Instructions
1. Pull the latest code: `git fetch origin && git merge origin/agent/devops --no-edit`
2. Run all tests: `npx vitest run`
3. Run type check: `npx tsc --noEmit`
4. Review the changes: `git log origin/main..HEAD --oneline`
5. Verify the implementation matches the task description
6. If ALL checks pass: mark as PASS
7. If ANY check fails: mark as FAIL with clear reason

## Auto-chain (MANDATORY)
```bash
AGENT_DIR=$(basename "$(pwd)")
node ~/Desktop/Custom\\ Connectors/Cortex/cortex-telegram-hub-bot/scripts/agent-complete.js --agent $AGENT_DIR --verdict pass --summary "describe what you validated"
```
Or if FAIL:
```bash
AGENT_DIR=$(basename "$(pwd)")
node ~/Desktop/Custom\\ Connectors/Cortex/cortex-telegram-hub-bot/scripts/agent-complete.js --agent $AGENT_DIR --verdict fail --reason "describe the failure"
```

## Notion Task ID
336ad49d-23e7-8199-a1e0-d7b2b24766b1