node_modules/
dist/
data/*
!data/classifier-golden-set.json
.env
.env.*
# Local Docker sandbox env template — tracked; private .env.local is not.
!.env.local.example
*.db
*.db-journal
*.db-wal
logs/
.local/
.DS_Store
.claude/
# prompts/ — tracked (needed for autoresearch)
content-engine/.venv/
content-engine/__pycache__/
content-engine/data/
content-engine/**/__pycache__/
# Test output
coverage/
reports/
*.review.json
# Agent task files (auto-generated by dispatch-tasks.js)
.agent-task.json
.agent-prompt.md
.env.agents
# Engineering-excellence enrichment (2026-05-04, ENG-EXC-O3): generated
# evidence files from cannot-skip-gate-dashboard.sh. Each run produces
# a timestamped JSON file; pinning these in git would make every run
# noisy. Operator can re-generate any time.
docs/release/cannot-skip-gate-evidence/

# Operator-local signed rollback drill evidence. The payload includes the
# current git SHA, so committing it would immediately make the signature stale.
docs/release/evidence/rollback-drill-latest.json
