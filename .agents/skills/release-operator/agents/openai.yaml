interface:
  display_name: "Nexus Release Operator"
  short_description: "Prepare and verify exact backend release artifacts"
  default_prompt: "Prepare or inspect a Nexus Hub backend release without deploying unless explicitly authorized."
