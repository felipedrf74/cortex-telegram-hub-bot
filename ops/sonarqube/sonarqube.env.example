# Existing /home/dominguez/sonarqube/.env variable names. Keep the live file
# outside Git and preserve its current values.
POSTGRES_USER=replace-with-existing-user
POSTGRES_PASSWORD=replace-with-existing-password
POSTGRES_DB=replace-with-existing-database
