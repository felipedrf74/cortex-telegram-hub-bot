# Open Items

Status: canonical
Owner: release lead (Felipe)
Last verified: 2026-07-14
Update policy: keep only current carryovers here. Historical closed, fixed, and
deferred rows for the 2026-05 sweep live in
`docs/release/OPEN_ITEMS_ARCHIVE_2026-05.md`. Rotate monthly with
`engine/scripts/rotate-open-items.mjs`.

## Current Wave 1 Operator Gates

These require Felipe/operator action or physical devices. They are not Codex
closable without explicit authorization and live credentials/devices.

- Signed TestFlight archive/upload from Xcode.
- Two-account physical walkthrough on Felipe + Jaqueline devices.
- Real provider-state validation for Gmail/Outlook/Health/Garmin on devices.
- First-48-hours Garmin tenant-isolation watcher observation after cohort start.
- App Store metadata / privacy nutrition label review before broad Wave 1.
- Training UX round manual simulator matrix (light/dark, Dynamic Type XXXL,
  pt-PT/pt-BR/en on new copy, wearable states, provider-revoked routing,
  decision deep-link anchoring, Home readiness suffix) plus the fresh-Marathon
  onboarding pass — before the TestFlight cut (folded-in Q3 residual sweeps).

## Current Engineering Carryovers

| ID | Severity | Area | Status | Next step |
| --- | --- | --- | --- | --- |
| IOS-TRAINING-RELEASE-GATE | P1 | iOS release | READY / OPERATOR PROMOTION PENDING | iOS `58069db` (`1.5.0`, build 55) is the committed local release candidate with the authorized Training feature bundle and local Release/UI evidence. Apple Distribution and App Store Connect access are configured; paid hosted CI is not required for this release path. Push the reviewed source to iOS `main`, archive/upload build 55, wait for TestFlight processing, run the physical-device Training/media smoke, and expire build 54 only after the replacement is green. No TestFlight or device proof is claimed yet. |
| TRN-PUBLIC-BETA-PROMOTION | P1 | training release | READY / OPERATOR PROMOTION PENDING | Backend `fff8cd8e` (`4.14.218`) and iOS `58069db` (`1.5.0`, build 55) are committed local release candidates. Deploy the exact backend commit to staging; apply and verify migration 231; run Training/calendar/Decision/media/APNs smoke; back up and execute the owner-authorized Training-state cleanup; promote the exact artifact to production; activate package `51c1089c…74bb` bound by release subject `27b97ebc…cea3` at `https://media.nexushub.me`; then verify public health, PM2, media, Decision Center, Training, and owner-device behavior. Production DB/runtime activation and TestFlight remain pending. |
| AI-COST-ENFORCEMENT | P1 | paid AI rollout | OPEN / BEFORE FLAG ACTIVATION | Backend code and migration 226 are live on production 4.14.216 at `6c67c181`; iOS source is on main at `8c48d7a`. Production is intentionally observe-only because `PAID_AI_COST_CONTROLS_ENFORCEMENT_ENABLED` is unset. Before enabling it, run synthetic Free, trial, active Pro/Max, founder, owner, beta/manual, expired, and canceled-at-period-end personas on staging; prove daily/monthly rollover, concurrency, dormant Points, automation ceilings, no background Points debit, token-zero availability, and additive REST/WebSocket/iOS compatibility; then obtain explicit owner authorization for the environment flag change. |
| AI-COST-LIVE-QUALITY | P1 | paid AI quality | OPEN / AUTHENTICATED PROVIDER + DEVICE EVIDENCE REQUIRED | Before broad enforcement, run live-provider Coach/Content/Channel/Autoresearch quality checks and signed-device quota UX/APNs proof. Confirm Coach structured parsing/citations/calendar actions and eligible delivery, Content batch validity, Channel category/actionable-pattern parity, stable provider-denial behavior, and no comparison fallback after a budget denial. Local deterministic tests and the 25/25 unauthenticated staging smoke do not substitute for live-provider quality evidence. |
| AI-COST-30D-ACCEPTANCE | P1/P2 | paid AI economics | BLOCKED BY ELAPSED OBSERVATION WINDOW | Starting only after an authorized eligible rollout, collect 30 days of entitlement-scoped usage and consumption instrumentation. Accept when background spend stays within 30% of both windows, Coach p95 input falls at least 40% from 10,469 with no quality regression, Autoresearch calls fall at least 85%, unchanged Channel/full Content inventory consume zero model calls, and cost per delivered/consumed artifact falls at least 40% at comparable paid activity. |
| AI-COST-P3-FOLLOWUPS | P3 | paid AI architecture | DEFERRED / OWNER PRODUCT DECISIONS | Preserve the fix-round dispositions in engine `docs/agents/handoffs/2026-07-10-paid-ai-cost-controls-fix-round.md`. Intentional owner sign-off: Points checkout stays 403 in flag-off mode. Decisions still needed for portal manual-relearn attribution (recommend `automation`), non-Secretary WebSocket token-zero parity, OCR denial ordering, distributed plan-cap overrides, missing search-fee billing, Ollama preflight ordering, failure-as-delivery semantics, fixer retry cost, iOS cold-launch cache window, and Free-to-Secretary hardcode. Default-tenant shared-knowledge evidence, cross-tenant p95, and Python re-entry TOCTOU limitations are documented risks, not hidden completion claims. |
| TRN-SKILL-E2E-VALIDATION | P1 | training | CONTAINER LANE DONE 2026-07-02 / LIVE-CALENDAR DEFERRED | Isolated container lane executed on main (run `training-e2e-20260702143816-f0c3fc3e`, 23 iOS tests / 22 passed, evidence at engine `.local/training-e2e/`); it caught and drove the learning-path persistence fix (`ae75d178`, on the prod lineage). Residual: (1) infra-flaky `kAXErrorIPCTimeout` fixture test — separate stabilization task filed; (2) live-provider calendar lifecycle writes — blocked on Felipe provisioning sandbox Google/Outlook accounts + non-prod refresh tokens and setting `NEXUS_TRAINING_E2E_LIVE_CALENDAR_ACK=sandbox-non-prod-calendar` (gate verified at `training-e2e-up.sh:56`). |
| TRN-REDFLAG-PRODUCER | P2 | training | DEFERRED 2026-07-02 / ROADMAP TRIGGER (re-verified at engine 2330f2b3 / iOS c79e42a: still the sole producer, still dormant) | Red-flag safety surfacing shipped on iOS (`TrainingSignal` gates on `safety_red_flag` family, 38e4927/94be966), but the only backend producer is `publishSafetyRedFlag` via structured health intake (`training-coach-v2.ts` `/health-intake/red-flag`), which is feature-flag gated and never called by iOS — the state is dormant in production paths. Decision recorded 2026-07-02: keep dormant for now; the roadmap option is shipping structured-intake UI plus enabling the coach-v2 flag. Deriving red flags from completion `painScore` was rejected (consent-scope implications). |
| TRN-DEADLETTER-VISIBILITY | P2 | training | BACKEND SHIPPED 2026-07-02 / iOS HALF OPEN | Backend exposure shipped to prod (commit `ae75d178`, in deploy `6bb2affe`): additive `calendarCleanup: { deadLetteredCount } \| null` on the plan read models (current-week + all-weeks incl. no-plan path) in `training-read-models.ts`, counting `secretary_agenda_items` rows with owner + tenant + `source_skill='training'` + `provider_sync_state='delete_failed'` + `provider_sync_failure_count >= 5` (the linkage-predicate condition resolved YES via `source_skill`); null-honest (zero / pre-migration / error → null) and never via home `meta.reasonCodes`. Remaining: no iOS surface consumes the field — add a Plan-zone maintenance note using the "needs re-connect" vocabulary, never "conflict". |
| REL-EVIDENCE-PERSISTENCE | P2 | release process | OPEN (reduced 2026-07-02) | The evening 2026-07-02 promote DID persist staging-smoke evidence (`staging-smoke-cfb5f33f-20260702T163904Z.json`, committed in deploy commit `6bb2affe`) — but manually: the standalone smoke wrote it AFTER the staging rsync, tripping the promote's drift guard until the operator committed it and re-deployed staging. The 2026-06-29 and morning-2026-07-02 (`f0c3fc3e`) promotes remain without persisted evidence (historical, accepted). Residual: pipeline the persistence — smoke-evidence files are NOT in the artifact digest (`release-artifact-manifest.mjs` digests dist/migrations/prompts/runtime only; the abort is the git-cleanliness guard), so a scripted pre-promote smoke → commit → redeploy-staging step closes this. `promote-to-prod.sh`'s internal smoke stays evidence-off by design. |
| REL-DRYRUN-AUTHOR | P2 | release hygiene | CONFIG FIXED 2026-07-02 / HISTORICAL RESIDUAL | Root cause was NOT the release-prep flow: the engine repo-local `.git/config` carried a leftover `[user] Release Dry Run Test <test@example.invalid>` block, leaked from the dry-run harness (`__tests__/scripts/release-deploy-dry-runs.test.ts:70-71`) before its `cleanGitEnv` scrub landed 2026-06-25 (`4a75448e`); no `scripts/` code sets it. It misauthored ~126 engine commits 2026-06-04 → 2026-07-02 (incl. release preps `f0c3fc3e`/`c99ac80c` and deploy commit `6bb2affe`) — far beyond release-prep. The local config block was removed 2026-07-02; effective identity verified back to Felipe's global. Residual: history is not rewritten (accepted — commits are pushed/deployed). Closing check: verify the 4.14.212 prep commit is correctly authored. |
| PHASE2B5-CHAT-FASTPATH | P2 | chat architecture | PARKED / REOPEN TRIGGER ONLY (trigger re-checked 2026-07-02: NOT fired) | Reopen only if beta usage shows fastpath cache/coalescing bugs, a third real fastpath implementation appears, or a planned feature needs a unified iOS/Telegram/WebSocket fastpath. 2026-07-02 audit: chat-core-v2's new deterministic-read path was checked against the archived trigger definition (command-cache keys + pending-task cache TTL + in-flight dedup, per `docs/archive/2026-05/phase2b5-chat-fastpath-dedup/closeout-deferred.md`) — it is stateless composition with none of those mechanics, so the trigger has NOT fired. Cleanup while nearby: `src/services/chat-write-intent-fastpath-guard.ts` is orphaned (zero src importers; superseded by action-gateway `shouldGateReadFastPathsForWriteIntent`) — delete when convenient. |
| GAP-CAL-1 | P1 | secretary | PARTIAL — 9 crons remain; silent-failure risk (audited 2026-07-02) | `conflict_detection` emits NotificationIntent (`scheduler.ts:1584/:1595`, commit `5bbe1b40`); reminders, shared_list, training_plan_adjust and channel_relearn dual-emit; briefings/coach/end-of-day use the durable report+APNs path. Nine cron paths still notify via legacy `safeSend` only — and `safeSend` is a NO-OP unless `TELEGRAM_LEGACY_DELIVERY='true'`, so with the flag off they notify nobody: invoice_collection (:1422), amazon_collection (:1504), uber_collection (:1535), invoice_queue flush (:1882), fossa_email confirmation (:1573), seo_agent (:1961), autoresearch (:1970/:1981), db_backup (:1995), db_restore_test failure alert (:2011). Worst case: a failed weekly restore test is fully silent (no throw → the job-failure operator alert never fires). Migrate db_restore_test + db_backup first. |
| GAP-FIN-2 | P1 | finance | OPEN / OWNER DECISION REQUIRED | Decide non-Brazil finance jurisdiction model and behavior contract. |
| GAP-REL-6 | P1 | mock lint | OPEN / OWNER DECISION REQUIRED + STRICT GATE BREACH 2026-07-02 | Partial-mock count is 1042 (387 test files / 175 modules) vs strict baseline 999 — the CI strict lint (`ci.yml` "vi.mock completeness lint" `--strict`) fails at engine HEAD `2330f2b3`. Re-verified 2026-07-06 at HEAD `ca901c4a`: 1059 (391 test files / 178 modules, 15 unparseable factories) — 60 over baseline and still growing. Stale ceiling numbers in `docs/skills/tdd/SKILL.md` (827) and AGENT_PROCESS_STANDARD §9.7 (640) were reconciled 2026-07-06 to defer to `engine/scripts/.vi-mock-baseline.txt` as the only authoritative figure. Trajectory 660 (2026-05-05) → 999 (2026-06-06 recalibration) → 1042 → 1059: never toward <100, which is now ~943 away with the 2026-08-01 commitment ~30 days out. Two actions: (1) restore the gate green — complete the 43 overage or recalibrate the baseline with rationale; (2) decide — authorize a codemod-driven batch (`scripts/codemod-mock-factory.mjs`; `src/services/database.ts` alone = 248 findings) with a realistic date, or formally revise/drop <100-by-2026-08-01 (backlog's "~70 months at 10/month" is now ~94). |
| GAP-REL-7 | P1 | release gate | OPEN / OWNER DECISION REQUIRED | Decide whether two-account E5 becomes a hard CI/deploy gate or remains operator evidence. |
| CF-BOT-MGMT-1 | P2 | Cloudflare edge tooling | OPEN (re-verified 2026-07-02: script unchanged since c04200c9) | Update `scripts/cloudflare-edge-unblock.mjs` to use a focused Free-plan-safe Bot Management payload (today `applyBotManagement` spreads the full GET response into the PUT, which Free plan rejects with 400) so operators no longer need `--skip-bot-management` plus manual focused `curl`. Prod edge posture already correct via the documented workaround. |
| IOS-PAYWALL-AUTORENEW | P0 | iOS billing | OPEN / TESTFLIGHT BLOCKER (found 2026-07-02 audit) | `PaywallView.swift` (iOS main `c79e42a`) shows price + period and the legal-consent checkbox, but NO auto-renewable subscription disclosure (renewal period, price-per-period statement, cancel-anytime language) required by App Store Review 3.1.2; repo-wide grep finds no renewal copy. The 2026-06-05 "fix" was verified only on the retired scaffold checkout (`codex/refined-onboarding-ios` in the desktop backup) and never landed in the canonical repo. Ship disclosure copy (pt-PT/pt-BR/en) before the signed TestFlight / Wave 1 cut. |
| SEC-ENVAGENTS-ROTATION | P0/P1 | security ops | OPEN / OPERATOR VERIFY REQUIRED | 2026-06-05 QA found prod `.env.agents` world-readable (644) containing the OAuth master encryption key + iOS JWT secret. Deploy tooling was hardened since (umask 077, chmod 600 on artifacts), but `deploy.sh:625` EXCLUDES `.env.agents` from rsync — deploys never touch the file, and no chmod/rotation evidence exists in any commit, doc, or row. The 2026-07-02 audit could not verify server state (secret-file inspection over SSH denied in-session). Operator: run `stat -f '%Lp %Su' /home/dominguez/telegram-hub-bot/.env.agents` (GNU: `stat -c '%a %U'`); if not `600 dominguez`, chmod AND rotate both exposed keys, then record the result here. |
| DC-DISMISS-FAILOPEN | P2 | decision center | OPEN (last surviving 2026-06-15 P1; re-verified at 2330f2b3) | `dismissNotificationCenterItem` (`notification-orchestrator.ts:1448-1458`) UPDATEs only rows with status IN ('unread','read') and never checks `result.changes`; `dismissDecision` (`decision-center.ts:2716-2733`) then emits a `dismissed` lifecycle event and records `{dismissed:true, actionSucceeded:true}` even when the UPDATE no-opped (snoozed/failed items). `performNotificationAction` guards superseded/dismissed/actioned but not snoozed. Effect: dismissing a snoozed decision reports success while the item stays snoozed and the outcome ledger records a dismissal that never happened. Fix: check changes, fail closed (or include snoozed in the dismissable set deliberately). |
| GDPR-EXPORT-HEALTH-DECRYPT | P2 | privacy | OPEN (found 2026-07-02 audit) | The Article 15 export (`settings.ts:303`) SELECTs `apple_health_data` raw; since health-at-rest encryption (`8254d61c`, live in prod) rows carry `data_json='{"encrypted": true}'` + ciphertext in `encrypted_data_json`, so users receive redacted placeholders instead of their health data. Wire the export path through `parseAppleHealthDataJson` (as readiness/health readers already do). Deletion (Article 17) is unaffected and verified working. |
| REL-PROMOTE-LOCK-RECLAIM | P1 | release gate | OPEN / FIX CHIP FILED (task_cc5809e5) | `scripts/lib/release-gates.sh` stale-lock reclaim is self-defeating when the lock's `owner` file is missing/misnamed (observed real case: `owner 2`): the pid staleness path is unreachable, and creating the `.reclaiming` marker INSIDE the lock dir refreshes the very mtime the second staleness check reads (lines 164-230) → promote aborts (exit 73) until a manual `rm -rf`. Wedged the 2026-07-02 evening promote (attempt 1; owner pid verified dead before manual clear). Fix: snapshot the lock mtime before marker creation or move the marker outside the lock dir; keep pid+host checks primary. |
| REL-VERSION-MINT | P2 | release process | OPEN — DO AT NEXT RELEASE PREP | `4.14.211` spans TWO production artifacts (morning `f0c3fc3e`, evening `6bb2affe`; disambiguate by deploy commit / artifact digest `2e75b0e0…`). The next release prep MUST mint `4.14.212` so the version uniquely identifies an artifact again — and verify the prep commit is authored by Felipe (closing check for REL-DRYRUN-AUTHOR). |
| BILL-RECEIPT-UNIQUE-BACKSTOP | P3 | billing | OPEN | Cross-account Apple receipt reuse is blocked app-level (`stripe-service.ts:709-725` → 409 `APPLE_TRANSACTION_ALREADY_CLAIMED`, in prod), but `subscriptions` has NO `UNIQUE(provider, provider_subscription_id)` index (migration 050 defines a non-unique one; none of 051-220 adds uniqueness) — the check-then-insert window is only safe while PM2 runs a single fork. Add the unique index + conflict handling before scaling beyond one instance. |
| NOTIF-RELEASE-CAS | P3 | notifications | OPEN | The 2026-06-15 double-send core fix is in (due-row SELECT filters `sent_at IS NULL`; cron + event-backbone converge on one release path), but `updateReleasedLogs` (`notification-orchestrator.ts:1009-1037`) lacks an `AND sent_at IS NULL` compare-and-set — two invocations overlapping across the async push await can both send the same row. Rare under single-fork PM2; add the CAS clause. |
| IOS-LIQUID-GLASS-AUDIT | P3 | iOS chrome | DEFERRED (recorded durably 2026-07-02; was only in the session plan file) | Audit custom chrome under SDK 26 / Liquid Glass (no deployment-target change involved). Deferred during the 2026-07-02 Training UX round. |
| IOS-TRAININGVIEW-DECOMP | P3 | iOS training | DEFERRED (recorded durably 2026-07-02; was only in the session plan file) | Decompose `Views/Training/TrainingView.swift` (3,478 lines at `c79e42a`) into per-zone modules (zone views exist; the root file still holds routing, builder reducers, and fixtures). Schedule once the container E2E lane (TRN-SKILL-E2E-VALIDATION) is stable. |

## Recently Closed

| ID | Severity | Area | Closed | Evidence |
| --- | --- | --- | --- | --- |
| TRN-M5-MEDIA-PUBLICATION | P1 | training media | 2026-07-14 | Publication-preparation governance closed for 158 canonical exercises and 200 selected mappings. All six reviews are complete for package `51c1089cceb8a916abf200b5cb3688b19f5f7553990467ee0f8ef01c7c4f74bb`, release subject `27b97ebc96e1b3bb1ee3612e63c5609b5572c9d4b58e59b8ea3e77642fb1cea3`, and approved origin `https://media.nexushub.me`. Actual production publication/runtime activation remains open under TRN-PUBLIC-BETA-PROMOTION. |
| AI-COST-CODE-PROMOTION | P1/P2 | paid AI rollout | 2026-07-10 | Backend 4.14.216 deployed through staging and production from `6c67c181`; promotion smoke passed 25/25; strict deploy verification passed 873 files / 12,910 tests; public health, PM2 version/status, migration-226 single application, 236 total migration records, and artifact digest `13ff241c…d95` matched. Backend and iOS `origin/main` are `6c67c181` and `8c48d7a`. Enforcement flag activation remains separately open under AI-COST-ENFORCEMENT. |
| TR-REDESIGN-GATES | P1 | training redesign | 2026-07-02 | The zone redesign (Today/Plan/Progress + coach reasoning sheet) shipped: iOS `training-redesign/retirement` is an ancestor of `main` (main at `38b3bb5` carries the zone IA plus 18 follow-up Training commits), and the paired backend Phase-0 endpoints are live in prod `4.14.211`. Gate history: Q0 PASS; Q1/Q2 executed with findings; Q3 last formal verdict was NO-GO (2026-06-12), and its blockers were subsequently fixed individually on main — `de7d170`/`013bec1` (day-anchor, carousel, gate-card id), `2d54ce1` (light-mode Progress picker contrast), `2926be8` (history unit pairing), `b4c359b`/`3a90f7b` (Athlete Profile questionnaire CTA), and engine `training.ts` energy-from-fatigue derivation (pinned by `training-routes.test.ts` rerun-5/6 S12 tests). No formal Q3 re-run happened; residual manual sweeps (wearable matrix, pt-PT/pt-BR runtime, keep-original loop) are folded into the 2026-07-02 Training UX round manual matrix. Parked v1.1 follow-ups remain listed in the Q3 work order. |
| TRN-REMEDIATION | P1/P2 | training | 2026-06-03 | Code remediation executed end-to-end across backend Training coach, chat/parser/action registry, plan generation, safety/load math, sport engines, lifecycle/routes, iOS decoding/state/rendering, and low-adherence product surface. Evidence: backend `npm run verify` passed typecheck, science-policy pin check, and full Vitest **807 files / 11,794 tests**; `NODE_ENV=development TELEGRAM_BOT_TOKEN=local-training-eval-token-disabled TELEGRAM_ALLOWED_USER_IDS=1 npm run eval:training` passed with score **99/100** across **156** cases; iOS simulator build passed; focused iOS Training suite passed **113/113**; full `scripts/ios-single-simulator-test.sh` passed **1,451 XCTest tests** plus **10 Swift Testing cases**. Remaining signed TestFlight/device, HealthKit/Watch, Garmin, provider-state, APNs, and two-account proof stays in the operator gates above. |
| GAP-TRN-1 | P1 | training | 2026-05-12 | Engine `b35ed604` runs strict write-free plan-linter preflight before cancellation/persistence and returns `plan_quality_blocked` without plan/session/calendar writes; iOS `d337636` renders that state as failed/requires-review instead of created. Focused backend 36/36, route 31/31, broad Training 908/908, iOS focused 14/14, and broader iOS Training subset 85/85 passed. |
| GAP-REL-1 | P1 | docs/identity | 2026-05-12 | Re-ran `engine/scripts/release-identity.sh markdown --persist --quiet`; `docs/release/release-identity.md` now points at backend `feature/decision-center-logic-v2` commit `9d34f3fd` and iOS `feature/decision-center-logic-v2` commit `460850c`. Docs audit rerun after mirror refresh. |
| GAP-REL-2 | P1 | docs/release | 2026-05-12 | Reconciled workspace current release state so 4.14.149 Round E + Decision Center remains the active production truth and older 4.14.132 notes are marked historical. |
| W1-FIN-COLLECTORS | P1/P2 | finance collectors | 2026-05-12 | Added collector-service owner-scope enforcement before Amazon/Uber global browser session or Playwright launch paths, preserving the existing Telegram command and owner-tier scheduler gates. Broad per-user collector sessions remain a separate future product/credential workstream. |
| GAP-CONT-1 | P0/P1 | content runtime | 2026-05-12 | Portal channel writes now reject client-supplied owned-channel markers unless ownership is server-verified, and YouTube creator analytics scope resolution ignores unverified manual metadata. Backend live performance aggregation and iOS performance dashboard truth are now wired under GAP-CONT-3/GAP-CONT-4. |
| GAP-CONT-3 | P1 | content loop | 2026-05-12 | Content performance aggregate now reads tenant-scoped live `content_performance` metrics for recent views, retention, likes, comments, subscriber gain, top items, and retention warnings instead of stopping at radar feedback. Focused aggregate tests cover live metrics and user isolation. |
| GAP-CONT-4 | P1 | iOS content | 2026-05-12 | Verified existing iOS content performance surface from commit `d91b3ec`: `ContentIntelligenceView` renders views, retention, likes, comments, subscriber gain, and top performer from `performanceSummary`; focused `ModelDecodingTests` for summary/detail performance payloads passed 2/2. |
| ROUND-B-FIXTURE | P2 | staging fixture | 2026-05-12 | Added `--felipe-volume-calendar` / `--calendar-events 100` staging fixture mode. Synthetic reserved users can now seed 100 DB-backed read-only calendar events consumed by `unified-calendar`, calendar routes, and dashboard timing probes without real OAuth/provider writes. Operator still needs to run staging before/after evidence. |

## Authorization-Gated Codex Workstreams

| Queue | Status | Notes |
| --- | --- | --- |
| Batch 25 | pending authorization | Content lifecycle unification phase 1: audit and plan. |
| Batch 26 | pending authorization | Content lifecycle schema migration phase. |
| Batch 27 | pending authorization | Content lifecycle service/API migration phase. |
| Batch 28 | pending authorization | Content lifecycle iOS/Python contract and cleanup phase. |

## Evidence Gaps To Preserve

- Batch 17 iOS P2 remediation report was not reconstructable in the Batch 24
  archive pass. Revalidation exists at
  `docs/archive/2026-05/tech-debt-validation/codex-batch-17-revalidation.md`.
- Batch 1 does not have a standalone remediation artifact; evidence is
  preserved in
  `docs/archive/2026-05/tech-debt-validation/codex-tech-debt-pass.md` and
  `docs/archive/2026-05/tech-debt-validation/codex-validation-matrix.md`.
