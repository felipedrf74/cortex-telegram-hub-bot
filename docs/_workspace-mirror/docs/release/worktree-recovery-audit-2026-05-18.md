# Worktree Recovery Audit — 2026-05-18

Status: completed; cleanup batches 1-2 applied
Owner: Felipe
Generated by: Codex
Artifact root: `docs/release/worktree-recovery-audit-2026-05-18/artifacts/`

## Summary

- Audited 40 worktree entries: 24 backend and 16 iOS.
- Preserved dirty work before any cleanup recommendation. Each worktree has an artifact directory with metadata, status, diffs, untracked manifests, and handoff copies where present.
- Initial audit made no deletions or promotions. Cleanup batches later removed only `delete-safe`, `prod-shipped`, and archive-only/done worktrees after artifacts were captured; branches and remotes were preserved.
- Confirmed backend production via read-only SSH: prod package version is `4.14.170`; `nexus-hub` and `content-engine` are online.
- Backend `origin/main` is `ee780102`; production deploy version commit is `65ed74f9` (`4.14.170`) with docs-only commits after it.
- iOS `origin/main` is `e3908cc`.

## Cleanup Execution — 2026-05-18

Completed after Felipe approved following the recommendations:

- Pruned missing backend/iOS worktree metadata, including the stale `/private/tmp` entries.
- Removed backend worktrees: `beta/single-agent-rc`, `codex/home-orchestration-focus`, `feat/phase-16-chat-presentation`, `beta/rc`, `claude/dazzling-wu-779176`, `claude/elated-bouman-b5a808`, `feature/content-creation-workflow-ui-codex-validation`, `fix/p0-tenant-and-safety-2026-05`, `fix/perf-and-a11y-2026-05`, and `feature/skill-hardening-engine-codex-20260517`.
- Removed iOS worktrees: `feature/cooking-rich-state-ui`, `codex/home-orchestration-focus`, `feat/phase-16-chat-presentation`, `fix/p0-tenant-and-safety-2026-05`, and `fix/perf-and-a11y-2026-05`.
- Preserved all local branches and remote branches. Branch deletion remains a separate cleanup step if Felipe wants the refs removed after another review.
- Left all `merge-candidate` and `archive-only` worktrees in place.

Post-cleanup worktree counts:

- Backend after batch 1: 12 worktrees remained.
- iOS after batch 1: 10 worktrees remained.

Cleanup batch 2 applied after Felipe confirmed done/archive-only worktrees could also be removed:

- Removed backend archive-only worktrees: `beta/gap-2-tenant-isolation`, `beta/gap-3-auth-onboarding`, `beta/gap-5-admin-sessions`, `beta/gap-7-observability`, `feature/training-validation-and-coach-fixes`, `beta/gap-6-integration-truth`, and `feature/training-reliability-local-orchestration-hardening-codex-20260503`.
- Removed iOS archive-only worktrees: `beta/gap-7-observability`, `beta/single-agent-rc`, `feature/tech-debt-2026-05-k2-ios-date-formatters`, and `feature/tech-debt-2026-05-n1-preview-runtime-polarity`.
- Final local worktree lists are captured at `artifacts/post-cleanup-worktrees-20260518-batch2.txt`.

Final worktree counts:

- Backend: 5 worktrees remain (`main` plus 4 merge candidates).
- iOS: 6 worktrees remain (`main` plus 5 merge candidates).

## Preservation Artifacts

Canonical artifact files:

- `artifacts/summary.json` — worktree metadata, dirty counts, upstreams, ahead/behind counts, artifact paths.
- `artifacts/branch-classification.tsv` — every local and `origin/*` branch with containment and unique-commit counts versus `origin/main`.
- Per-worktree folders — `metadata.txt`, `status-full.txt`, `status-tracked.txt`, `tracked-worktree.diff`, `tracked-index.diff`, `untracked-files.txt`, and `commits-not-in-origin-main.patch` where applicable.
- Small untracked sets were also preserved as `untracked-files.tar.gz`.

Recovery examples:

- Apply tracked dirty work: `git apply <artifact-dir>/tracked-worktree.diff`
- Replay unmerged commits: `git am <artifact-dir>/commits-not-in-origin-main.patch`
- Restore archived untracked files: `tar -xzf <artifact-dir>/untracked-files.tar.gz -C <worktree>`

## Worktree Disposition

| repo | branch/worktree | state | classification | disposition |
|---|---|---:|---|---|
| backend | `main` | dirty, behind `origin/main` by 9 | archive-only | Do not promote local dirty state; archive is captured. Fast-forward/reset local main to `origin/main` only after Felipe confirms. |
| backend | `/private/tmp/nexus-p0-base` | missing/prunable | delete-safe | Prune missing worktree metadata; equivalent live worktree exists at `fix/p0-tenant-and-safety-2026-05`. |
| backend | `/private/tmp/nexus-portal-redesign` | missing/prunable | archive-only | Prune missing worktree metadata only; keep branch `feature/nexus-hub-portal-uiux-admin-user-console` archived because it has unique commits. |
| backend | `beta/gap-2-tenant-isolation` | dirty deletion-only | archive-only | Dirty state is mass deleted docs/prompts/knowledge; preserve, do not merge. |
| backend | `beta/gap-3-auth-onboarding` | dirty deletion-only | archive-only | Same beta residue pattern; preserve, do not merge. |
| backend | `beta/gap-5-admin-sessions` | dirty deletion-only + 1 unique commit | archive-only | Preserve only. Requires explicit review before any cherry-pick. |
| backend | `beta/gap-7-observability` | dirty deletion-only | archive-only | Preserve, do not merge. |
| backend | `beta/single-agent-rc` | clean, contained | delete-safe | Local worktree can be removed after archive; branch can remain as historical beta reference. |
| backend | `codex/home-orchestration-focus` | clean, contained in prod | prod-shipped | Backend work is in `4.14.169+`; worktree can be removed after ledger/docs confirmation. |
| backend | `feat/phase-16-chat-presentation` | clean code, untracked smoke evidence | prod-shipped | Code is in prod. Preserve untracked smoke evidence before removing worktree. |
| backend | `feat/test-infra-scoped-runner` | clean, 3 unique commits | merge-candidate | Highest-priority non-prod infra candidate; pair with iOS test-infra before merge. |
| backend | `feature/training-validation-and-coach-fixes` | clean, 3 old unique commits | archive-only | Superseded-looking April training branch; keep archived unless Felipe revives it. |
| backend | `codex/training-google-validation` | dirty real training code/tests | merge-candidate | Important uncommitted training work. Needs branch cleanup commit, classifier, typecheck, and focused/full training tests. |
| backend | `beta/rc` | clean, contained, very stale | delete-safe | Remove local worktree after archive. |
| backend | `beta/gap-6-integration-truth` | untracked duplicate `* 2.ts` files | archive-only | Preserve duplicate files; likely Finder/conflict residue, not merge-ready. |
| backend | `claude/dazzling-wu-779176` | clean, contained, stale | delete-safe | Remove local worktree after archive. |
| backend | `claude/elated-bouman-b5a808` | clean, contained, stale | delete-safe | Remove local worktree after archive. |
| backend | `feature/content-creation-workflow-ui-codex-validation` | clean, contained in main | delete-safe | Local worktree can be removed; branch is already represented on main. |
| backend | `feature/training-reliability-local-orchestration-hardening-codex-20260503` | clean, 1 old unique commit | archive-only | Preserve as historical training hardening, not current prod candidate. |
| backend | `fix/home-perf-pre-wave1-2026-05` | clean, 2 unique commits | merge-candidate | Pair with iOS home-perf; rebase and rerun focused Home tests if revived. |
| backend | `fix/p0-tenant-and-safety-2026-05` | clean, contained/exact remote | delete-safe | Local worktree can be removed; preserve branch/remote until final janitor approval. |
| backend | `fix/perf-and-a11y-2026-05` | clean branch, untracked smoke evidence | delete-safe | Preserve evidence tar; then worktree can be removed. |
| backend | `feature/skill-hardening-engine-codex-20260517` | prod head, untracked duplicate `* 2` files | prod-shipped | Committed work is production `4.14.170`; preserve duplicate untracked files, then clean/remove worktree. |
| backend | `feature/training-revamp-engine-codex-20260516` | clean, 4 unique commits | merge-candidate | Active training revamp candidate; watch migration collision before merge. |
| iOS | `main` | dirty scheme + untracked test infra files | archive-only | Preserve; do not treat as shipped source. Compare against test-infra branch before deciding whether to keep. |
| iOS | `/private/tmp/nexus-ios-phase2b4` | missing/prunable | archive-only | Prune missing metadata only; branch has unique work and should remain archived. |
| iOS | `beta/gap-7-observability` | dirty deletion-only docs/skills | archive-only | Preserve, do not merge. |
| iOS | `beta/single-agent-rc` | dirty deletion-only docs | archive-only | Preserve, do not merge. |
| iOS | `feature/cooking-rich-state-ui` | clean, contained in main | delete-safe | Remove local worktree after archive. |
| iOS | `codex/home-orchestration-focus` | clean, contained in main | prod-shipped | Source is in iOS main; worktree can be removed after archive. |
| iOS | `feat/phase-16-chat-presentation` | clean, contained in main | prod-shipped | Remove local worktree after archive. |
| iOS | `feat/test-infra-scoped-runner` | clean, 1 unique commit | merge-candidate | Pair with backend test-infra; run focused runner validation before merge. |
| iOS | `feature/tech-debt-2026-05-k2-ios-date-formatters` | clean, 1 unique commit | archive-only | Low-priority tech-debt patch; preserve, do not promote by default. |
| iOS | `feature/tech-debt-2026-05-n1-preview-runtime-polarity` | clean, 1 unique commit | archive-only | Low-priority tech-debt patch; preserve, do not promote by default. |
| iOS | `feature/tech-debt-2026-05-n2-content-xcuitest-pack` | dirty content code/tests | merge-candidate | Useful test/content hardening candidate; needs cleanup commit and focused Content tests. |
| iOS | `fix/home-perf-pre-wave1-2026-05` | clean, 1 unique commit | merge-candidate | Pair with backend home-perf candidate. |
| iOS | `fix/p0-tenant-and-safety-2026-05` | clean, contained/exact remote | delete-safe | Remove local worktree after archive. |
| iOS | `fix/perf-and-a11y-2026-05` | clean, contained/exact remote | delete-safe | Remove local worktree after archive. |
| iOS | `feature/skill-hardening-ios-codex-20260517` | dirty finance/training tests/code | merge-candidate | Possible iOS follow-up to backend skill hardening; needs branch cleanup and focused Finance/Training tests. |
| iOS | `feature/training-revamp-ios-codex-20260516` | clean, 1 unique commit | merge-candidate | Pair with backend training revamp candidate. |

## Promotion Queue

1. **Test infrastructure scoped runner**
   - Backend: `feat/test-infra-scoped-runner`
   - iOS: `feat/test-infra-scoped-runner`
   - Why: high leverage, recent, coherent, directly supports safer cleanup and future merges.
   - Required gates: backend typecheck + focused script tests; iOS `scripts/ios-changed-area-runner.sh`; verify pre-commit behavior; update docs if accepted.

2. **Training revamp**
   - Backend: `feature/training-revamp-engine-codex-20260516`
   - iOS: `feature/training-revamp-ios-codex-20260516`
   - Why: active recent feature pair with staging evidence.
   - Required gates: migration collision check, backend classifier/typecheck/verify, iOS focused Training tests, staging deploy/smoke before any prod ask.

3. **Training Google validation dirty work**
   - Backend: `codex/training-google-validation`
   - Why: substantial uncommitted training correctness changes and tests.
   - Required gates: convert dirty work to a review branch, inspect overlap with training revamp, run focused Training/Calendar suites, then decide whether to merge or cherry-pick.

4. **Home performance pre-wave1**
   - Backend: `fix/home-perf-pre-wave1-2026-05`
   - iOS: `fix/home-perf-pre-wave1-2026-05`
   - Why: small paired performance patch, but older than Home Orchestration and must be rebased carefully.
   - Required gates: rebase on current `origin/main`, run Home/dashboard focused suites, inspect for conflicts with Home Orchestration.

5. **iOS finance/skill hardening follow-up**
   - iOS: `feature/skill-hardening-ios-codex-20260517`
   - Why: dirty but meaningful Finance/Training changes; backend side is already production `4.14.170`.
   - Required gates: cleanup commit, focused Finance/Training tests, confirm it is still compatible with iOS `main`.

6. **iOS content XCUITest pack**
   - iOS: `feature/tech-debt-2026-05-n2-content-xcuitest-pack`
   - Why: dirty Content test/code changes may still be useful.
   - Required gates: cleanup commit, focused Content tests, decide whether it belongs with test-infra or Content work.

## Cleanup Checklist

Completed cleanup:

- Pruned missing worktree metadata for `/private/tmp/nexus-p0-base`, `/private/tmp/nexus-portal-redesign`, and `/private/tmp/nexus-ios-phase2b4`.
- Removed clean contained and prod-shipped local worktrees listed in the cleanup execution section.
- Removed worktrees with only archived untracked/evidence residue where artifacts had already been captured.
- Removed archive-only/done worktrees after artifacts had already captured their dirty diffs, untracked files, and unique commits.

Needs Felipe approval:

- Any local branch deletion.
- Any remote branch deletion.
- Any reset/fast-forward of dirty backend local `main`.
- Any production promotion.
- Any decision to revive old beta branches or old tech-debt branches.

Keep active:

- All promotion queue branches.
- Backend and iOS `origin/main`.
- Any branch referenced by an `in_worktree`/active ledger row until the ledger is corrected.

## Verification Performed

- `git fetch origin --prune` for backend and iOS.
- Read-only SSH confirmed prod backend package version `4.14.170`.
- `curl https://api.nexushub.me/health` returned healthy.
- Worktree metadata, diffs, untracked manifests, tarballs, and format patches generated under the artifact root.
- No typecheck, test suite, staging deploy, smoke, branch deletion, or production promote was run during this audit.

## Follow-up Actions

1. Merge or archive the test-infra candidate first; it reduces future merge/test risk.
2. Pick one training lane: training revamp or training Google validation, then reconcile overlap.
3. Decide whether to delete local branch refs for the removed worktrees.
4. Refresh backend local `main`, which is still dirty and behind `origin/main`.
5. Decide whether to retire the remaining home-perf, content-XCUITest, and iOS skill-hardening candidates after review.
