[Service]
# Permanent bootstrap guard: a restart must consult the durable install
# journal even if power is lost before the candidate override is published.
ExecStartPre=+/usr/local/sbin/nexus-ollama-install-state-check.mjs
