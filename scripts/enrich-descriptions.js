#!/usr/bin/env node
const fs = require('fs');
const path = require('path');
let T = '';
try { T = fs.readFileSync(path.resolve(__dirname, '.env.agents'), 'utf8').match(/NOTION_TOKEN=(.+)/)?.[1]?.trim(); } catch {}
try { if (!T) T = fs.readFileSync(path.resolve(__dirname, '..', '.env.agents'), 'utf8').match(/NOTION_TOKEN=(.+)/)?.[1]?.trim(); } catch {}
if (!T) { console.error('No token'); process.exit(1); }

async function upd(id, desc) {
  const r = await fetch(`https://api.notion.com/v1/pages/${id}`, {
    method: 'PATCH',
    headers: { 'Authorization': `Bearer ${T}`, 'Content-Type': 'application/json', 'Notion-Version': '2022-06-28' },
    body: JSON.stringify({ properties: { Description: { rich_text: [{ text: { content: desc.substring(0, 2000) } }] } } }),
  });
  return r.ok;
}


const UPDATES = [
// ═══ Architecture ═══
['335ad49d-23e7-8109-8575-f7c526db6c7e', 'Per-user data isolation with AES-256-GCM encryption for financial data. SecureStore class in src/utils/encryption.ts: encrypt(userId, data) / decrypt(userId, data). HKDF key derivation with unique salt per user from FINANCE_ENCRYPTION_KEY env var (256-bit master key). Each user\'s data encrypted at rest in SQLite. Export capability: /export my-data → generates JSON file for download. Delete capability: /delete my-data → GDPR-compliant data removal. Files: src/utils/encryption.ts (SecureStore), src/services/finance-tracker.ts (integrate encryption), src/skills/credentials.ts. Tests: encrypt/decrypt round-trip, per-user isolation (user A can\'t read user B).'],
['335ad49d-23e7-81f7-83e0-e6e18dad0739', 'Replace cron-based API polling with webhooks for real-time event delivery. Create IntegrationProvider interface with: registerWebhook(), handleEvent(), verifySignature(). Webhook receiver at POST /api/webhooks/:provider with HMAC-SHA256 signature verification using WEBHOOK_SECRET env var. Adapters: Garmin (activity push), Strava (event subscription API), Google Calendar (push notifications via /watch), Outlook (change notifications via Graph API subscriptions). Event bus in src/services/webhook-registry.ts stores events in SQLite webhook_events table with provider, event_type, payload, received_at. Max payload 1MB (WEBHOOK_MAX_PAYLOAD). 30-day retention (WEBHOOK_RETENTION_DAYS). Files: src/services/webhook-registry.ts, src/portal/server.ts (POST routes), src/config.ts (webhook config). Tests: signature verification, event dedup, payload size limit.'],
['335ad49d-23e7-81d5-b56e-ea9feacb6876', 'Telegram HTML template system for consistent, branded message formatting. Create src/utils/telegram-templates.ts with template functions: briefingTemplate(data), expenseTemplate(data), calendarTemplate(data), trainingTemplate(data), mealPlanTemplate(data). Only supported Telegram HTML tags: <b>, <i>, <u>, <code>, <pre>, <a href>, <blockquote>. NO tables/CSS/colors/images inline. Message limit 4096 chars — auto-split with splitMessage(). For charts: use chartjs-node-canvas to render server-side PNG, send as ctx.replyWithPhoto(). Each template has a header section, body sections with emoji indicators, and footer. Standardize across all domains. Files: src/utils/telegram-templates.ts (templates), src/utils/telegram-formatter.ts (escapeHtml, splitMessage). Tests: template output matches expected HTML, XSS prevention, message length compliance.'],
['335ad49d-23e7-8152-b4c8-e71d1f5e6974', 'Content Agent Mesh v2: upgrade Intelligence Bus so content agents share learnings and cross-reference. Shared memory layer in src/state/shared-memory.ts — key-value store with domain tags, TTL, and cross-domain queries. Tools: shared_memory_set(key, value, domain), shared_memory_get(key), shared_memory_query(domain). Token budget per agent: daily cap configurable via skill sub-skill config. User-configurable schedules: content discovery cron expression per user. Cross-agent learning: when fitness agent logs a PR, content agent can reference it for content ideas. Files: src/state/shared-memory.ts, src/services/intelligence-bus.ts, src/skills/skill-config.ts (token budgets). Tests: cross-domain memory access, TTL expiry, token budget enforcement.'],
['334ad49d-23e7-8119-bf05-e63b9d27dff1', 'SDK for third-party skill developers. Package: @nexushub/skill-sdk published to npm. API: createSkill(manifest, handlers) → SkillPackage. Helper functions: defineTools(toolDefs), defineCommands(cmdDefs), defineCronJobs(jobDefs), definePrompts(promptDefs). SkillManifest type: { name, version, description, author, domains, tools, commands, cronJobs, prompts, migrations, dependencies }. Runtime: skills loaded by SkillLoader at boot, registered in SkillRegistry. Isolation: each skill gets namespaced DB tables (skill_{name}_*), no cross-skill data access. Files: src/sdk/index.ts (main exports), src/sdk/types.ts (interfaces), src/sdk/create-skill.ts, src/sdk/define-tools.ts, src/sdk/define-commands.ts. Tests: SDK creates valid skill package, manifest validation, type checking. Docs: README.md with quickstart guide.'],
// ═══ Backend Features ═══
['335ad49d-23e7-81d5-a758-fb91e1876a81', 'Interactive Telegram questionnaires with Grammy inline keyboard buttons. Three questionnaire types: fitness (10 questions: training experience 1-10, goals checkboxes, available equipment multi-select, weekly hours slider, injury history, preferred sports), diet (8 questions: dietary preference, allergies multi-select, cooking skill level, weekly budget, meal prep willingness, cuisine preferences), homeschool (12 questions: child age, curriculum type, subjects, daily schedule hours, learning style, parent availability). Flow: /onboarding <type> → bot sends question 1 with InlineKeyboard buttons → user taps answer → callback triggers question 2 → ... → profile saved to user_profiles table (user_id, type, answers JSON, completed_at). Re-trigger: /onboarding <type> overwrites previous profile. Files: src/services/onboarding.ts (questionnaire engine), src/bot.ts (register /onboarding command + callback handlers). DB: user_profiles table.'],
['335ad49d-23e7-81d8-89f9-e2d63ac14877', 'Cooking Chef skill with Spoonacular API integration. Commands: natural language "receita de frango" detected by classifier → cooking domain. /recipe <query> → searches Spoonacular findByIngredients or complexSearch → shows top 3 results with inline buttons to select. Selected recipe: shows ingredients, step-by-step instructions, prep/cook time, servings. /mealplan → generates 7-day plan based on user diet profile from onboarding (carnivore preference, allergies). After meal plan, prompt "Criar lista de compras?" with Yes/No inline buttons → Yes creates Microsoft To Do task "Compras da Semana" with checklist items for each ingredient + quantities. API key: SPOONACULAR_API_KEY env var. Files: src/services/cooking-chef.ts (API client + meal planner), src/domains/cooking.ts (domain handler), prompts/cooking.md (system prompt). DB: meal_plans table. All text PT-BR.'],
['335ad49d-23e7-8100-9f72-f24abeb11952', 'AI-generated training plans based on fitness profile + Garmin data. Readiness scorer: analyzes sleep score, HRV, body battery, stress, recovery from Garmin API → outputs readiness 1-10. Weekly auto-adjust: if readiness <5, reduce volume 20%. If >8, allow intensity increase. Calendar blockers: create Outlook events for each training session (title: "🏋️ Upper Body A", time from profile preferred slots). Cross-reference: compare planned vs actual by matching calendar events against Garmin activities (by time overlap). Show discrepancies: "Planned: 5km run at 07:00, Actual: 3km walk at 07:15 — under-delivered". Commands: "show training plan" → this week\'s schedule. "adjust today\'s training" → AI recommends based on current readiness. Files: src/services/training-plans.ts (plan generator + readiness scorer), src/services/garmin-coach.ts (Garmin data analysis), src/services/unified-calendar.ts (event creation). Tests: readiness calculation, auto-adjust logic, calendar event creation.'],
['335ad49d-23e7-81f2-9678-ea7cadb7c489', 'Invoice Manager NLP rules: natural language rule creation. User says "faturas da EDP chegam todo mês" → creates rule: vendor=EDP, periodicity=monthly, pattern=electricity. Rule engine: when new invoice arrives via SSH scan, match against rules to auto-categorize. Configurable periodicity: monthly, quarterly, semi-annual, annual, custom cron. Rule CRUD: create via natural language, list via /invoice rules, delete by ID. Reminder: if expected invoice hasn\'t arrived by expected date +3 days, send Telegram notification. Files: src/services/invoice-filer.ts (rule matching), src/services/invoice-collector.ts (SSH scanner + rule check), src/state/invoice-vendors.ts (rule storage). DB: invoice_rules table (id, vendor, pattern, periodicity, next_expected, created_at). All text PT-BR.'],
['334ad49d-23e7-816b-82f6-f2b5d80396f8', 'SkillLoader service: dynamically loads skill packages at boot and runtime. Reads skill manifests from installed_skills table → validates manifest schema → registers tools, commands, cron jobs, and prompts. Hot-reload: watches skill files for changes, re-registers without restart. Dependency resolution: skills can depend on other skills (manifest.dependencies), loader verifies deps are installed+enabled before loading. Isolation: each skill runs in its own namespace, no shared state. Files: src/skills/loader.ts. Interface: load(skillId), unload(skillId), reload(skillId), getLoaded(). Tests: load valid skill, reject invalid manifest, dependency resolution, hot-reload.'],
['334ad49d-23e7-81ef-bf8c-fea029e20b8a', 'SkillRegistry service: SQLite-backed state management for installed skills. Table: installed_skills (id TEXT PK, name TEXT, version TEXT, enabled BOOLEAN, config JSON, installed_at TEXT, updated_at TEXT). Methods: install(manifest), uninstall(skillId), enable(skillId), disable(skillId), getInstalled(), getEnabled(), isEnabled(skillId), updateConfig(skillId, config). Sub-skill state: skill_submodules table (skill_id, submodule_name, enabled). Seed DEFAULT_SKILLS on first boot. Files: src/skills/registry.ts. Tests: CRUD operations, enable/disable toggle, sub-module state persistence, concurrent access.'],
['332ad49d-23e7-8128-ac76-d6faca73c9a4', 'TelegramAdapter: Grammy-based implementation of MessageAdapter interface. Methods: sendText(chatId, text, parseMode), sendFile(chatId, file, caption), sendInlineButtons(chatId, text, buttons), editMessage(chatId, msgId, newText), deleteMessage(chatId, msgId), sendPhoto(chatId, photo, caption), sendVoice(chatId, audio). Handles: HTML parse mode with fallback to plain text on parse errors, message splitting for >4096 chars, rate limiting (30 msg/sec per chat). Error handling: retry on 429 (Too Many Requests) with exponential backoff. Files: src/adapters/telegram-adapter.ts (implementation), src/adapters/message-adapter.ts (interface). Tests: mock Grammy bot, verify send methods, rate limit behavior.'],
['332ad49d-23e7-81b9-8d32-d0552dd5170a', 'WhatsApp Business API adapter implementing MessageAdapter interface. Methods: sendText(chatId, text), sendFile(chatId, file, caption), sendInlineButtons(chatId, text, buttons → interactive list/buttons), editMessage(not supported → send new), deleteMessage(chatId, msgId). WhatsApp API specifics: webhook verification (GET /webhooks/whatsapp with verify_token), incoming message webhook (POST /webhooks/whatsapp), message templates for outbound initiation, media upload via media API endpoint. Auth: WHATSAPP_PHONE_ID, WHATSAPP_ACCESS_TOKEN env vars. Files: src/adapters/whatsapp-adapter.ts (implementation), src/portal/server.ts (webhook routes). Tests: webhook signature verification, message formatting, template rendering.'],
];


const UPDATES2 = [
// ═══ Infrastructure ═══
['335ad49d-23e7-810b-87b0-d04680145009', 'Health check endpoints for Docker HEALTHCHECK and external monitoring. GET /health → { status: "ok"|"degraded", uptime: seconds, db: "ok"|"error", bot: "polling"|"stopped", memory: process.memoryUsage(), version: package.json version }. Returns 200 if healthy, 503 if degraded. GET /health/detailed?token=HEALTH_TOKEN → adds: cron job statuses (from telemetry.ts getJobStatuses), integration health (OAuth token expiry dates), error count last hour, last message timestamp. Protected by HEALTH_TOKEN env var — returns 401 without token. Used by: Docker HEALTHCHECK CMD curl -f http://localhost:8200/health, UptimeRobot (free tier, ping every 5min). Files: src/portal/server.ts (add routes), src/config.ts (HEALTH_TOKEN). Tests: 200 on healthy, 503 on degraded, 401 without token.'],
['335ad49d-23e7-81a4-85e8-ff3264ae8d94', 'Error monitoring with @sentry/node SDK. Initialize in src/index.ts before bot starts: Sentry.init({ dsn: SENTRY_DSN, environment, release: package.json version }). Capture: unhandled rejections + uncaught exceptions (automatic), explicit captureException() in catch blocks for AI API failures, OAuth token expiry, cron job failures, Telegram delivery errors. Tags per error: domain, agent, userId (hashed), skill. Breadcrumbs: AI API calls, DB queries, cron jobs. Alert: on new error type, send Telegram notification to admin. Create src/services/error-monitor.ts with init(), captureException(err, context), captureMessage(msg). Source maps uploaded to Sentry for readable stack traces. Portal: show error count in KPI bar. Files: src/services/error-monitor.ts, src/index.ts (init), src/portal/server.ts (error count API). Free tier: 5K errors/month.'],
['335ad49d-23e7-81b9-b707-c6039b8c2d1e', 'Daily SQLite backup with cron job. Schedule: BACKUP_TIME env var (default "03:00" = 3 AM). Backup dir: BACKUP_DIR env var (default ./backups/). Rotation: keep last BACKUP_RETENTION_DAYS (default 30) files, auto-delete older. Filename: nexushub-backup-YYYY-MM-DD-HHmmss.sqlite3. Process: (1) SQLite backup API (db.backup(destPath)), (2) verify backup integrity (open + pragma integrity_check), (3) delete old files. Encrypt backups if BACKUP_ENCRYPT=true using AES-256 with BACKUP_KEY. Weekly restore test: every Sunday, restore latest backup to temp file, run integrity check, delete temp. Cron job registered in scheduler.ts via registerJob("backup", ...). Files: src/services/backup.ts, src/services/scheduler.ts (register job). Tests: backup creates file, rotation deletes old, integrity check passes.'],
['332ad49d-23e7-81dc-a558-f11483e052cd', 'Deploy Nexus Hub to Hetzner Cloud EU. Current server: IPv6-only at 2a01:14:8021:c0d0:d5e8:b946:d3e4:a53b. Stack: Docker Compose (bot + portal on port 8200). Dockerfile: node:20-alpine multi-stage build, non-root USER node, .dockerignore excludes .env/.git/tests/node_modules. Docker Compose: bot service + nginx reverse proxy for TLS. Nginx: Let\'s Encrypt auto-renewal via certbot, TLS 1.2+ only, HSTS header, HTTP→HTTPS redirect. Deploy script: scripts/deploy.sh → rsync dist/ + package.json to server → docker compose up -d --build. PM2 inside container for process management. DNS: nexushub.me → A/AAAA records pointing to Hetzner. Firewall: only ports 80, 443, 22 open. SSH: key-only auth, non-default port. Files: Dockerfile, docker-compose.yml, nginx/nginx.conf, scripts/deploy.sh.'],
['334ad49d-23e7-810a-89db-cecc81e25168', 'Skill database migrations system. Each skill owns migrations prefixed with skill name: skill_{name}_001_create_table.sql. Migration runner in src/services/database.ts: on boot, scan all skill migrations, run pending ones in order. Migration table: _skill_migrations (skill_id, migration_id, applied_at). Rollback support: each migration has up() and down() SQL. Skills declare migrations in manifest: { migrations: ["001_create_expenses", "002_add_categories"] }. Isolation: skill can only create tables with skill_{name}_ prefix. Files: src/services/database.ts (migration runner), src/skills/loader.ts (trigger migrations on install). Tests: run migrations, rollback, prevent cross-skill table access.'],
['334ad49d-23e7-8133-a783-e8fde0ad6fd8', 'npm packaging for skill distribution. Convention: @nexushub/skill-{name} (e.g., @nexushub/skill-finance). Package.json template: { "name": "@nexushub/skill-finance", "version": "1.0.0", "nexushub": { "type": "skill", "manifest": "./manifest.json" }, "main": "./dist/index.js" }. Manifest.json: { name, version, description, domains, tools, commands, cronJobs, migrations }. Build: TypeScript → dist/ with type declarations. Publish: npm publish --access public. Install: /skill install @nexushub/skill-finance → npm install in skills/ dir → SkillLoader picks up. Files: scripts/skill-package-template/ (template files), src/skills/loader.ts (npm install integration). Docs: PUBLISHING.md guide for skill authors.'],
['334ad49d-23e7-819f-85b3-d824ebcc1b02', 'Namespaced migration system for skills. Each skill\'s migrations live in skills/{name}/migrations/ with prefixed naming: {skill}_{nnn}_{description}.sql. Migration runner scans all skill dirs on boot, tracks execution in _skill_migrations table (skill_id TEXT, migration_id TEXT, applied_at TEXT, PRIMARY KEY(skill_id, migration_id)). Ordering: global sort by (skill_id, migration_id). Conflict prevention: migration IDs must be unique within a skill. Skills can only CREATE/ALTER tables with their prefix (skill_{name}_*). Files: src/services/database.ts (namespace-aware runner), src/skills/loader.ts (scan skill migrations on load). Tests: run pending migrations, skip already-applied, block cross-namespace table creation.'],
// ═══ Security ═══
['334ad49d-23e7-817b-9d92-c2f02c1de398', 'Sandbox for third-party skills to prevent malicious code. Isolation layers: (1) DB namespace — skills can only access skill_{name}_* tables, enforced by SQLite trigger. (2) API access — skills declare required permissions in manifest (e.g., "permissions": ["calendar:read", "todo:write"]), runtime checks before tool execution. (3) No filesystem access — skills cannot read/write files outside their skill dir. (4) Token isolation — each skill gets its own encrypted credential store (src/skills/credentials.ts), no access to other skills\' tokens. (5) Rate limiting — per-skill API call limits (configurable in skill config). Files: src/skills/credentials.ts (isolated store), src/services/tool-executor.ts (permission checks), src/services/database.ts (namespace enforcement). Tests: cross-skill data access blocked, unauthorized permission rejected, rate limit enforced.'],
['334ad49d-23e7-81d7-a5e0-d84ed03e5392', 'Security audit for skill isolation. Verify: (1) SQLite tables use skill_{name}_ prefix — scan all CREATE TABLE statements. (2) Cross-skill data access blocked — test: skill A tries SELECT from skill_B_* table → must fail. (3) skill_config.ts DEFAULT_SKILLS have correct tool assignments — no tool bleeds across domains. (4) Credential store in src/skills/credentials.ts uses AES-256-GCM with per-skill key derivation. (5) SkillLoader validates manifest schema before loading (no arbitrary code execution). (6) Sub-skill toggle actually removes tools from AI tool array (test: disable sub-skill → tool not available). Files: src/skills/ (all files), src/services/database.ts (namespace checks). Output: SECURITY_AUDIT.md documenting findings + test results.'],
['335ad49d-23e7-8110-afa2-d2331633f7a5', 'Per-user rate limiting for bot commands and AI API calls. Config: config.rateLimit.maxMessagesPerMinute (default 30), config.rateLimit.maxAICallsPerHour (default 100). Implementation: in-memory Map<userId, { messageTimestamps: number[], aiCallTimestamps: number[] }>. Middleware in src/bot.ts: check before processing any message. If exceeded: reply "⚠️ Rate limit exceeded. Try again in X seconds." and drop the message. AI rate limit: check in src/services/anthropic.ts before each API call. Portal: show rate limit hits per user in admin view. Files: src/bot.ts (middleware), src/services/anthropic.ts (AI limiter), src/portal/server.ts (admin stats). Tests: exceed limit → message dropped, limit resets after window, concurrent messages handled correctly.'],
['335ad49d-23e7-8113-930b-f1f77595b345', 'Frontend security audit for portal and Mission Control. XSS prevention: audit every innerHTML assignment in src/portal/portal.html and scripts/mission-control.js — all dynamic data (task titles, agent names, job outputs from Notion/API) must pass through escapeHtml() or use textContent. CSP header: Content-Security-Policy: default-src \'self\'; script-src \'self\' \'unsafe-inline\'; style-src \'self\' \'unsafe-inline\'; img-src \'self\' data:. CORS: restrict Access-Control-Allow-Origin to localhost + nexushub.me (not wildcard *). Security headers: X-Content-Type-Options: nosniff, X-Frame-Options: DENY, Referrer-Policy: no-referrer. No eval() anywhere in client code. Files: src/portal/portal.html, scripts/mission-control.js (Express security headers). Tests: XSS payload in task title doesn\'t execute, CSP blocks inline script injection.'],
];


const UPDATES3 = [
// ═══ QA + Testing ═══
['334ad49d-23e7-81c5-8211-e2b0824ffe08', 'End-to-end command tests for the skill management system. Test scenarios: (1) /skills → lists all skills with status indicators (✅ enabled, ❌ disabled). (2) /skill disable secretary → disables secretary skill, confirm via /skills. (3) /skill enable secretary → re-enables, confirm secretary responds to messages. (4) /skill modules secretary → lists sub-modules (tasks, calendar, email, reminders). (5) /skill toggle secretary.email → disables email sub-module only. (6) Error cases: /skill disable nonexistent → graceful error message. (7) Permission check: non-admin user cannot toggle skills. Use Grammy test helpers to simulate Telegram messages. Each test: send command → assert bot reply matches expected pattern. Files: tests/e2e/skill-commands.test.ts. Mock: Grammy context, Notion API (for test isolation).'],
['334ad49d-23e7-814b-8fb3-e55eb4429fe0', 'Unit tests for SkillRegistry and SkillLoader services. Registry tests: install() creates DB record, uninstall() removes record, enable()/disable() toggles state, getEnabled() returns only enabled, isEnabled() checks specific skill, updateConfig() persists JSON config, seedDefaultSkills() populates on first boot. Loader tests: load() reads manifest + registers tools/commands/crons, unload() deregisters cleanly, reload() hot-swaps without restart, rejects invalid manifest (missing name/version), resolves dependencies (skill A depends on skill B → B loads first), circular dependency detection. Files: tests/unit/skill-registry.test.ts, tests/unit/skill-loader.test.ts. Mock: SQLite in-memory database, filesystem for manifest files.'],
['332ad49d-23e7-8120-8578-e5dd1521e99e', 'Integration tests for full message flow: Telegram input → classifier → domain handler → AI response → Telegram output. Test scenarios: (1) Plain text "what is on my calendar today?" → classified to secretary → calendar tool called → formatted response sent. (2) /expense 50 almoço → classified to finance → expense logged → confirmation sent. (3) "receita de frango" → classified to cooking → recipe search → results sent. (4) Ambiguous message → classifier uses Haiku → routes to most likely domain. (5) Tool execution loop: message triggers tool_use → tool executed → continueWithToolResults → final text response (NOT raw JSON). Files: tests/integration/message-flow.test.ts. Mock: Anthropic API (return scripted tool_use blocks), Grammy (capture sent messages), external APIs (calendar, todo).'],
['332ad49d-23e7-81a3-94f1-fcef21dc9cd6', 'Domain handler unit tests. Test each domain handler independently. Secretary: buildStateContext() returns correct format with todos/calendar/garmin data. handleSecretary() processes tool_use blocks correctly through the while loop. Tool execution: executeToolCall() routes to correct adapter. Finance: handleFinance() via handleSimpleDomain() processes expenses. Cooking: handleCooking() processes recipe queries. Triathlon: handleTriathlon() processes training queries. Content: handleContent() includes knowledgeBlock. Common assertions: (1) callDomain() called with correct system prompt. (2) Tool loop processes ALL tool_use blocks. (3) Final response is text, not raw JSON. (4) Conversation history updated. Files: tests/unit/domain-handler.test.ts, tests/unit/secretary.test.ts. Mock: Anthropic client, tool executor.'],
// ═══ Providers + Adapters ═══
['332ad49d-23e7-81d9-9a8b-f6277db08cad', 'SQLiteStorage implementation wrapping better-sqlite3 behind StorageProvider interface. Interface methods: get(key), set(key, value), delete(key), list(prefix), has(key), clear(). Table: kv_store (key TEXT PRIMARY KEY, value TEXT, updated_at TEXT). JSON serialization for complex values. Transaction support for batch operations. WAL mode for concurrent reads. File: src/storage/sqlite-storage.ts implementing src/storage/storage-provider.ts interface. Connection pooling: single db instance, lazy initialization. Auto-create table on first access. Tests: CRUD operations, JSON round-trip, prefix listing, concurrent access.'],
['332ad49d-23e7-810c-a4e1-ee23961f429a', 'Provider fallback with circuit breaker pattern for multi-AI resilience. Config via env: AI_CHAT_PRIMARY=claude, AI_CHAT_FALLBACK=gpt,gemini. AI_CLASSIFY_PRIMARY=haiku, AI_CLASSIFY_FALLBACK=gpt-mini. Circuit breaker per provider: states CLOSED (normal) → OPEN (3 consecutive failures, skip for 60s) → HALF_OPEN (retry one request). Track in memory: Map<provider, {failures, state, lastFailure}>. On API call: check circuit → if OPEN, skip to next → if CLOSED, try call → on failure, increment failures → if 3 consecutive, open circuit. Metrics: provider usage count, failure count, fallback trigger count (exposed via /health/detailed). Files: src/services/provider-fallback.ts (circuit breaker), src/services/provider-registry.ts (provider selection), src/config.ts (env vars). Tests: 3 failures → circuit opens, 60s → half-open retry, successful retry → circuit closes.'],
['332ad49d-23e7-81b7-9ab9-cbef5628a9dd', 'GeminiProvider implementation of AIProvider interface using @google/generative-ai SDK. Config: GEMINI_API_KEY env var. Models: gemini-2.0-flash (chat, cheap), gemini-1.5-pro (complex tasks). Methods: chat(messages, options) → maps Anthropic message format to Gemini format, classify(text, options) → lightweight classification. Message format mapping: Anthropic role "assistant" → Gemini role "model", tool_use blocks → Gemini function calling. System prompt: passed as systemInstruction. Token counting: response.usageMetadata. Error handling: catch GoogleGenerativeAI errors, map to standard error format for circuit breaker. Files: src/services/gemini-provider.ts implementing src/services/ai-provider.ts. Tests: message format mapping, token counting, error handling.'],
['332ad49d-23e7-814f-9d0e-f443303cd924', 'OpenAIProvider implementation of AIProvider interface using openai SDK. Config: OPENAI_API_KEY env var. Models: gpt-4o (chat), gpt-4o-mini (classification, cheap). Methods: chat(messages, options) → maps Anthropic message format to OpenAI format, classify(text, options) → lightweight classification with gpt-4o-mini. Message format mapping: Anthropic tool_use blocks → OpenAI function_call/tool_calls, Anthropic system prompt → OpenAI system message. Streaming: optional via options.stream. Token tracking: response.usage.prompt_tokens + completion_tokens. Files: src/services/openai-provider.ts implementing src/services/ai-provider.ts. Tests: message format mapping, function calling translation, streaming, error handling.'],
];


const UPDATES4 = [
// ═══ Product / SaaS ═══
['332ad49d-23e7-8141-abd3-e3d86d8c9fa5', 'WhatsApp Business API adapter implementing MessageAdapter interface. Methods: sendText(chatId, text), sendFile(chatId, mediaUrl, caption), sendInlineButtons(chatId, text, buttons → WhatsApp interactive list/reply buttons max 3), editMessage(not natively supported → send replacement with "Updated:" prefix), deleteMessage(chatId, msgId). WhatsApp specifics: webhook verification (GET /webhooks/whatsapp?hub.verify_token=VERIFY_TOKEN), incoming messages (POST /webhooks/whatsapp with signature verification using WHATSAPP_APP_SECRET), message templates required for first outbound contact (24h session window). Media: upload via POST /v17.0/{PHONE_ID}/media. Auth: WHATSAPP_PHONE_ID, WHATSAPP_ACCESS_TOKEN, WHATSAPP_APP_SECRET env vars. Files: src/adapters/whatsapp-adapter.ts, src/portal/server.ts (webhook routes), src/config.ts (WhatsApp config). Tests: webhook signature verification, session window enforcement, button rendering.'],
['332ad49d-23e7-811c-9222-ddf1bd5500ca', 'Module marketplace MVP at nexushub.me/marketplace. Browse and install community skills. Backend: GET /api/marketplace → list published skills from marketplace_skills table (name, description, author, downloads, rating, npm_package). POST /api/marketplace/install → runs npm install + SkillLoader.load(). Frontend: simple HTML page with card grid (skill name, description, author, install button). Submission: skill authors submit via PR to nexushub-marketplace GitHub repo with manifest.json. Review: Felipe approves PRs manually. Rating: users rate via /skill rate <name> <1-5> in Telegram. Files: src/portal/marketplace.html (UI), src/portal/server.ts (API routes), src/services/marketplace.ts (install logic). DB: marketplace_skills table, skill_ratings table.'],
['332ad49d-23e7-81cc-83f6-ef499948a85c', 'User-facing web dashboard at nexushub.me/dashboard. Auth: Telegram login widget (validates hash with BOT_TOKEN). Dashboard shows: active skills grid with enable/disable toggles, usage stats (messages today, AI calls, storage used), recent activity timeline (last 20 interactions across domains), quick actions (run morning briefing, check calendar, add expense). Data: all fetched from portal API endpoints with user token auth. Responsive: mobile-first (375px), Tailwind CSS via CDN. Files: src/portal/dashboard.html (UI), src/portal/server.ts (user-scoped API routes with auth middleware). No external framework — vanilla HTML/JS.'],
['332ad49d-23e7-81c4-a4e3-d097cbae045a', 'Stripe billing integration for managed SaaS. Three tiers: Starter (free, 50 AI msgs/day, 3 skills), Pro (€9.99/mo, 500 msgs/day, all skills, priority support), Enterprise (€29.99/mo, unlimited, API access, custom skills). Stripe setup: STRIPE_SECRET_KEY + STRIPE_WEBHOOK_SECRET env vars. Checkout: /api/billing/checkout → creates Stripe Checkout Session → redirects to Stripe. Webhook: POST /api/billing/webhook → handles checkout.session.completed, customer.subscription.updated/deleted. Usage enforcement: check tier limits before AI API call in src/services/anthropic.ts. DB: subscriptions table (user_id, stripe_customer_id, tier, status, current_period_end). Files: src/services/billing.ts (Stripe SDK), src/portal/server.ts (checkout + webhook routes), src/portal/pricing.html (pricing page). Tests: webhook signature verification, tier enforcement, upgrade/downgrade.'],
['332ad49d-23e7-8107-98f2-da7c6e373243', 'Browser-based onboarding wizard at nexushub.me/setup. Multi-step form: (1) Connect Telegram — show QR code / deep link to @Hlepreguica_bot. (2) Connect services — OAuth flows for Google Calendar, Outlook, Garmin. (3) Select skills — checkboxes for secretary, finance, cooking, triathlon, content. (4) Configure preferences — timezone, language (PT-BR/EN), notification schedule. (5) Complete — summary + "Send me a test message" button. Each step: form validation, progress bar, back/next buttons. Data saved via API calls to portal backend. Auth: Telegram login widget. Files: src/portal/onboarding.html (multi-step wizard), src/portal/server.ts (onboarding API endpoints). No framework — vanilla HTML/CSS/JS with step transitions.'],
['332ad49d-23e7-8101-a4cd-cf5be619695f', 'Landing page at nexushub.me in PT-BR targeting Brazilian men 18-35. Sections: Hero (headline "Seu assistente pessoal com IA no Telegram" + CTA "Começar Grátis"), Features (6 skill cards with icons: secretary, finance, fitness, cooking, content, custom), How it Works (3 steps: connect Telegram, choose skills, start using), Pricing (3 tiers from billing task), Testimonials (placeholder for beta user quotes), FAQ (8 common questions), Footer (links, social, legal). Tech: static HTML + Tailwind CSS CDN, mobile-first (375px breakpoint). SEO: meta tags, og:image, schema.org. Analytics: Plausible (privacy-friendly, no cookies). Files: src/portal/landing.html or public/index.html. Deploy: served by portal Express server at / route.'],
['332ad49d-23e7-8189-9be2-d4ef88545cf2', 'Trademark search and registration for "Nexus Hub". Strategy: (1) Free search on EUIPO eSearch Plus (euipo.europa.eu) for EU trademark conflicts. (2) Free search on INPI (inpi.gov.br) for Brazilian trademark conflicts. (3) Nice Classification: Class 9 (software), Class 42 (SaaS). (4) If clear: file EU trademark via EUIPO (~€850 for 1 class). (5) Monitor: set Google Alert for "Nexus Hub" + similar marks. Risk: "Nexus" is common in tech — may need to defend "Nexus Hub" as a whole. Files: docs/TRADEMARK.md documenting search results and registration status. This is a manual task — Felipe handles the actual filing.'],
// ═══ Remaining Features ═══
['335ad49d-23e7-8133-bf61-f6b854f53110', 'Health Tracker skill: medication reminders with snooze/confirm buttons, supplement stack logging (creatine, D3, K2, Omega-3, organ meat caps, magnesium glycinate, glycine, L-theanine, taurine), blood pressure tracking with trend visualization. Commands: /health meds → show today\'s medication schedule with ✅/⏰ buttons. /health supplements → log today\'s supplements taken. /health bp <systolic> <diastolic> → log blood pressure reading. /health report → weekly summary with trends. DB: health_medications (name, dosage, time, frequency), health_supplements (name, taken_at), health_bp (systolic, diastolic, timestamp). Reminders: cron job checks medication schedule, sends Telegram reminder at scheduled time. Files: src/services/health-tracker.ts, src/domains/health.ts. PT-BR text.'],
['335ad49d-23e7-813e-8d39-fd6e6ac40982', 'Book Club skill: reading progress tracker with yearly goals, AI-powered note-taking and summaries, reading schedule suggestions. Commands: /book add <title> by <author> → adds to reading list. /book progress <title> <page>/<total> → updates progress (shows progress bar). /book notes <title> <note text> → saves note with page reference. /book summary <title> → AI generates summary from accumulated notes. /book goal <number> → sets yearly reading goal. /book stats → shows: books this year vs goal, avg pages/day, current streak, reading speed trend. DB: books (title, author, total_pages, current_page, started_at, finished_at), book_notes (book_id, page, note, created_at), reading_goals (year, target, completed). Files: src/services/book-club.ts, src/domains/books.ts. PT-BR text.'],
['336ad49d-23e7-814b-af96-ddbd50845fe5', 'Portal admin-only access with HTTP basic auth or bearer token. Admin routes: all /api/* endpoints require Authorization header (Bearer PORTAL_TOKEN env var or Basic admin:PORTAL_PASSWORD). Non-admin per-user view: /dashboard/* routes authenticated via Telegram login widget (hash verification with BOT_TOKEN). User sees: their own skills, usage stats, recent activity. Admin sees: all users, global stats, agent dashboard, skill management, system health. Middleware: src/portal/auth.ts with isAdmin(req) and isAuthenticated(req) functions. Rate limit: 100 req/min per IP for unauthenticated routes. Files: src/portal/auth.ts (auth middleware), src/portal/server.ts (apply middleware to routes). Tests: unauthorized access returns 401, admin token grants full access, user token scopes to own data.'],
['335ad49d-23e7-81c2-a599-c719bac285dc', 'Telegram welcome flow for new users. When user sends first /start: (1) Welcome message with bot avatar and "Olá! 👋 Sou o Nexus Hub, seu assistente pessoal com IA." (2) Quick skill selector with inline keyboard: 4 buttons (📋 Secretária, 💰 Finanças, 🏋️ Fitness, 🍳 Culinária) — user taps to enable. (3) Interactive tutorial: 3-step guided walkthrough showing how to use the first enabled skill (send example command, show response format, explain voice messages). (4) Profile setup: ask timezone, language preference. (5) "Pronto! Experimente: diga \'o que tenho na agenda hoje?\'" State machine in src/services/onboarding.ts tracks user progress (welcome_state: started|skills_selected|tutorial_done|complete). Files: src/services/onboarding.ts, src/bot.ts (/start handler). DB: user_onboarding table.'],
['335ad49d-23e7-81eb-85ba-e5d0e409a5be', 'Portal adapter status panel showing connected messaging platforms. Telegram card: 🟢 Active — polling status (last poll time), bot username (@Hlepreguica_bot), total messages today, last message timestamp. WhatsApp card: 🟡 Planned — "Coming soon" placeholder with waitlist signup. Discord card: ⚪ Not configured — placeholder. Each card: platform icon, status badge (active/planned/inactive), key metrics, configure button. Auto-refresh every 30s via fetch(/api/adapters). Files: src/portal/portal.html (adapter section), src/portal/server.ts (GET /api/adapters returning adapter status JSON). Data source: bot.api.getMe() for Telegram status, config checks for others.'],
['334ad49d-23e7-81e5-9d8a-cc7cf2b66f45', 'Telegram command /skill install <npm-package> for runtime skill installation. Flow: (1) Validate package name (must match @nexushub/skill-* pattern). (2) npm install --save <package> in project root. (3) Read manifest from installed package. (4) Validate manifest schema (name, version, tools, commands required). (5) Run skill migrations. (6) Register in SkillRegistry. (7) Load via SkillLoader (register tools, commands, cron jobs). (8) Confirm to user: "✅ Skill {name} v{version} installed with {N} tools." Error handling: invalid manifest → rollback npm install. Already installed → show current version + offer update. Files: src/commands/skill-install.ts, src/skills/loader.ts (runtime load), src/bot.ts (register command). Tests: install valid package, reject invalid manifest, handle network errors.'],
// ═══ Remaining small tasks ═══
['332ad49d-23e7-81c4-9ad5-e365f6e1b1e1', 'Usage metering system tracking AI messages per user per day. DB: usage_metrics table (user_id INT, date TEXT, domain TEXT, ai_calls INT, input_tokens INT, output_tokens INT, tool_calls INT, cost_estimate REAL). Increment on every AI API call in src/services/anthropic.ts trackedCreate(). Daily rollup: cron job at midnight aggregates per-user totals. Enforcement: check usage before API call — if over tier limit, return "⚠️ Daily limit reached" message. Portal: /api/usage?user_id=X → returns usage history. Admin: /api/usage/all → global usage dashboard data. Files: src/services/usage-metering.ts (track + enforce), src/services/anthropic.ts (hook into trackedCreate), src/services/scheduler.ts (daily rollup cron). Tests: increment tracking, limit enforcement, rollup accuracy.'],
['332ad49d-23e7-8121-a0a8-cdf5f66936e1', 'ConfigProvider abstraction for per-tenant configuration (single-tenant now, multi-tenant ready). Interface: get(key, default?), set(key, value), getAll(), getTenantConfig(tenantId). Implementation: SQLite-backed in config table (key TEXT PK, value TEXT, tenant_id TEXT DEFAULT "default"). Default config: timezone, language, notification_schedule, enabled_skills, ai_model_preference, rate_limits. Override chain: env var → DB config → default. Files: src/config/config-provider.ts (interface), src/config/sqlite-config.ts (implementation). Migrate existing config.ts env-only approach to use ConfigProvider. Tests: get/set, default fallback, env var override, tenant isolation.'],
['332ad49d-23e7-81d0-ae81-c661554cd513', 'Rename package from cortex-telegram-hub-bot to @nexushub/core (NOTE: rename completed)/core. Update: package.json (name, description, repository, homepage, bugs), all import paths referencing old name, Docker image name in Dockerfile + docker-compose.yml, GitHub repo references in CI/CD workflows, deploy scripts, README badges. Do NOT break any imports — use find/replace across all .ts files. Verify: npm pack produces @nexushub-core-*.tgz. Files: package.json, Dockerfile, docker-compose.yml, .github/workflows/*.yml, scripts/deploy.sh, README.md. Tests: npm pack succeeds, all imports resolve, CI pipeline passes.'],
['335ad49d-23e7-8100-b0b2-c6ef5e09eae1', 'Portal database tables overview panel. Shows all SQLite tables with: table name, row count, schema (column names + types), last modified estimate. Collapsible detail view per table showing first 5 rows as preview. Dangerous tables (users, credentials) show row count only, no preview. Auto-refresh every 60s. Files: src/portal/portal.html (DB section), src/portal/server.ts (GET /api/database/tables → returns table list with counts, GET /api/database/tables/:name/preview → first 5 rows). Query: SELECT name FROM sqlite_master WHERE type="table", then SELECT COUNT(*) for each.'],
['332ad49d-23e7-8180-980e-dde8e92051af', 'Affiliate/referral program for creator community growth. Structure: 20% recurring commission on referred Pro/Enterprise subscriptions, tracked via unique referral codes. Implementation: referral_codes table (code TEXT UNIQUE, user_id INT, created_at), referral_conversions table (code TEXT, referred_user_id INT, subscription_id TEXT, commission_rate REAL, paid_out BOOLEAN). Flow: user gets code via /referral → shares link nexushub.me/?ref=CODE → new user signs up → cookie tracks referral → on paid conversion, log commission. Payout: monthly via Stripe Connect or manual. Dashboard: /referral stats → shows clicks, conversions, earnings. This is a late-stage feature — implement after Stripe billing is live.'],
['332ad49d-23e7-81af-83ca-c836551803fb', 'Recruit 3-5 community module contributors. Strategy: (1) Post in Brazilian dev communities (TabNews, dev.to/pt, Twitter/X dev circles). (2) Share @nexushub/skill-sdk docs + example skill template. (3) Offer: featured placement in marketplace + co-author credit. Target skills: Habit Tracker, Pomodoro Timer, Expense Splitter, Portuguese Vocabulary, Workout Logger. Provide: skill template repo, development guide, testing sandbox, direct Telegram support channel. This is a manual/community task — Felipe handles outreach.'],
['332ad49d-23e7-8190-83b1-d376a92b0516', 'Reserve social media handles for @nexushub brand. Platforms: Instagram (@nexushub.me or @nexushub_app), X/Twitter (@nexushub_app), YouTube (@NexusHubApp), TikTok (@nexushub.app), GitHub (already: felipedrf74/nexus-hub (NOTE: renamed)). Action: create accounts with consistent branding (same bio, avatar, link to nexushub.me). Bio template: "Seu assistente pessoal com IA no Telegram 🤖 | Produtividade, Finanças, Fitness, Culinária | nexushub.me". This is a manual task — Felipe handles account creation.'],
['332ad49d-23e7-8159-9037-e0d8027f25f5', 'Configure nexushub.me domain. DNS: A record → Hetzner server IPv4 (or AAAA for IPv6 2a01:14:8021:c0d0:d5e8:b946:d3e4:a53b). TLS: Let\'s Encrypt certificate via certbot with auto-renewal cron. Nginx: server_name nexushub.me www.nexushub.me, proxy_pass to localhost:8200 (portal Express server). Redirects: www.nexushub.me → nexushub.me (canonical). Routes: / → landing page, /dashboard → user dashboard, /setup → onboarding wizard, /marketplace → skill marketplace. HSTS header: Strict-Transport-Security max-age=31536000. Files: nginx/nexushub.conf, scripts/setup-domain.sh (certbot + nginx config). This is a DevOps/manual task.'],
['332ad49d-23e7-81b1-9caa-f8bdb6f11c60', 'Add MIT copyright headers to all 60+ source files. Format: "// Copyright (c) 2025 Felipe Dominguez. MIT License. See LICENSE." as first line of every .ts file in src/, scripts/, tests/. Skip: node_modules, dist, .git, data/. Script: scripts/add-headers.sh using sed or node script to prepend header if not already present. Verify: grep -rL "Copyright" src/ shows zero results after running. Also update LICENSE file to full MIT license text with "Felipe Dominguez" as copyright holder. Files: all .ts files in src/, scripts/, tests/. Idempotent: running twice doesn\'t add duplicate headers.'],
];


async function main() {
  const all = [...UPDATES, ...UPDATES2, ...UPDATES3, ...UPDATES4];
  console.log(`📝 Enriching ${all.length} task descriptions...\n`);
  let ok = 0, fail = 0;
  for (const [id, desc] of all) {
    const success = await upd(id, desc);
    if (success) {
      ok++;
      console.log(`  ✅ [${ok}/${all.length}] ${desc.substring(0, 50)}...`);
    } else {
      fail++;
      console.log(`  ❌ FAILED: ${id}`);
    }
    await new Promise(r => setTimeout(r, 350));
  }
  console.log(`\n${'═'.repeat(50)}`);
  console.log(`✅ Updated: ${ok}`);
  console.log(`❌ Failed: ${fail}`);
  console.log(`${'═'.repeat(50)}`);
}

main().catch(e => { console.error('Fatal:', e); process.exit(1); });
